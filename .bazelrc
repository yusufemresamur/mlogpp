build --cxxopt=-Wall --cxxopt=-Wextra --cxxopt=-Werror
build --cxxopt=-std=c++23

build:coverage --cxxopt='-fprofile-instr-generate' --cxxopt='-fcoverage-mapping' --cxxopt='-O0' --cxxopt='-ftest-coverage' --linkopt='-fprofile-instr-generate'

# GCC toolchain (f0rmiga/gcc-toolchain) — hermetic
# xbin/gcc is the linker driver (not g++), so libstdc++ must be linked explicitly
build:gcc --extra_toolchains=@gcc_toolchain_x86_64//:cc_toolchain
build:gcc --linkopt=-lstdc++